/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  // आपकी Repository का सटीक नाम
  basePath: '/bjpmuzwest',
  images: {
    unoptimized: true,
  },
};

module.exports = nextConfig;
